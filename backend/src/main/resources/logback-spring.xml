<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Spring Boot 3.x Logback JSON encoder (logstash-logback-encoder 7.4) -->
    <!-- Structured JSON logging OpenTelemetry trace_id/span_id-vel + RequestIdFilter request_id MDC -->

    <!-- ===== Production profile: Structured JSON ===== -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <fieldName>timestamp</fieldName>
                        <timeZone>UTC</timeZone>
                        <pattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</pattern>
                    </timestamp>
                    <version/>
                    <logLevel/>
                    <loggerName/>
                    <threadName/>
                    <message/>
                    <stackTrace>
                        <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                            <maxDepthPerThrowable>30</maxDepthPerThrowable>
                            <rootCauseFirst>true</rootCauseFirst>
                        </throwableConverter>
                    </stackTrace>
                    <mdc>
                        <!-- A RequestIdFilter request_id-ja itt jelenik meg -->
                        <!-- Az OpenTelemetry tracing trace_id/span_id-je is MDC-ből jön -->
                    </mdc>
                    <pattern>
                        <pattern>
                            {
                                "service": "device-storage-backend"
                            }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>

    <!-- ===== Development profile: Human-readable ===== -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <!-- A %X{request_id} a RequestIdFilter MDC-ből jön -->
                <pattern>%d{HH:mm:ss.SSS} [%thread] [request_id=%X{request_id:-N/A}] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ===== Application loggers ===== -->
    <logger name="hu.tanszek" level="DEBUG"/>
    <logger name="org.hibernate.SQL" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.flywaydb" level="INFO"/>
    <logger name="io.opentelemetry" level="INFO"/>
</configuration>