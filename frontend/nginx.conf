# Frontend Nginx Configuration - PLACEHOLDER
# A tényleges Nginx config a Task 1.7 + Task 4.9-ben lesz implementálva.
# A részletes Nginx config (security headers, SPA fallback, gzip, /api/ reverse proxy, static asset caching)
# megtalálható: implementation_plan.md §4.

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Placeholder location — Task 1.7-ben lesz kibontva
    location / {
        try_files $uri $uri/ /index.html;
    }
}