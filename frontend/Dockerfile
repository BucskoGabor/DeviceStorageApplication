# Frontend Dockerfile - PLACEHOLDER
# A tényleges implementáció a Task 1.7-ben történik (Vite + React + Nginx build).
#
# Várható struktúra (Task 1.7):
#   FROM node:20-alpine AS build
#   WORKDIR /app
#   COPY package*.json ./
#   RUN npm ci
#   COPY . .
#   RUN npm run build
#
#   FROM nginx:1.25-alpine
#   COPY --from=build /app/dist /usr/share/nginx/html
#   COPY nginx.conf /etc/nginx/conf.d/default.conf
#   EXPOSE 80
#   CMD ["nginx", "-g", "daemon off;"]

# Jelenlegi placeholder
FROM alpine:3.19
RUN echo "Frontend Dockerfile placeholder — implementálás a Task 1.7-ben"
CMD ["echo", "Frontend not yet implemented (Task 1.7)"]