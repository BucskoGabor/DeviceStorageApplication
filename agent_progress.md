# Agent Haladási Napló (Progress Tracker)
## Projekt: Egyetemi Informatikai Tanszéki Nyilvántartó Rendszer

> **Utolsó frissítés:** 2026-07-29
>
> **Státusz jelölések:** `[ ]` backlog · `[~]` in progress · `[x]` done · `[!]` blocked · `Started: YYYY-MM-DD` az in-progress taskok mellett
>
> **Definition of Done (single-dev, self-reviewed):**
> 1. A code meg van írva és commitolva.
> 2. Unit és/vagy integration tesztek megírva és zölden futnak (ahol releváns).
> 3. Self-review: saját PR, checklist végignézve (naming, edge cases, biztonság, i18n kulcsok szinkronban, row-level filter minden műveletre).
> 4. **SOLID elvek betartása (a §3.0 Tervezési Elvek alapján):** SRP (minden service egy felelősség), OCP (AuthProvider interface, @RequirePermission aspektus), DIP (repository-k interface-en át injektálva, CryptoService mock-olható). Code review checklist a Task 5.5 CI workflow-ban.
> 5. Manuális smoke teszt a lokális docker-compose környezetben.
> 6. Dokumentáció: Javadoc a public service metódusokon, Springdoc OpenAPI annotációk a REST endpointokon, README frissítés ha az architektúra változik.

---

## Összesítő

| Metrika | Érték |
|---|---|
| Összes task | 33 |
| Kész | 33 |
| in progress | 0 |
| Backlog | 0 |
| Blocked | 0 |
| Haladás | 100% |

---

## Next 3 Task (Indítási Sorrend)

1. **Task 1.8:** Repo skeleton (`backend/`, `frontend/`, `docs/`, `scripts/` mappák, `.env.example`, `.gitignore`, `.editorconfig`, `LICENSE`, `CONTRIBUTING.md`, `README.md`, `bootstrap.sh`). Először ez kell, mert a bootstrap.sh a Task 1.8-ból generálja a `.env` fájlt a `.env.example` alapján, és a docker-compose `env_file: .env` direktívája erre a fájlra hivatkozik — nélküle a konténerek nem indulnak el megfelelő konfigurációval.
2. **Task 1.1:** `docker-compose.yml` elkészítése (5 konténer: postgres, backend, frontend, backup [backup_data volume], mailhog-dev). Ez a kiindulópont, minden más erre épül.
3. **Task 1.2:** Spring Boot projekt init (párhuzamosan az 1.7 frontend init-tel).

_Ezek egymás után: 1.8 (alapok) → 1.1 (compose) → 1.2/1.7 párhuzamosan, mert 1.2 szükséges az 1.3-hoz (Flyway dependency), és 1.1 szükséges a későbbi konténer-build tesztekhez._

---

### Fázis 1: Infrastruktúra, Adatbázis & JPA Entitások

- [x] **Task 1.1:** `docker-compose.yml` elkészítése (5 konténer [postgres, backend, frontend, backup, mailhog-dev], **explicit volumes szekció**: postgres_data, uploads_data, backup_data, audit_archive_data, healthcheckek, non-root userek [security_opt: no-new-privileges], resource limits, log rotation [json-file max 10m/3 files], mailhog **profiles: [dev]** host port mapping `1025:1025` + `8025:8025`, backend **read_only: true + tmpfs mount-ok /tmp és /var/run** + `env_file: .env` direktívával + uploads_data:/var/uploads + audit_archive_data:/var/backups/archive/audit, backup konténer **env_file: backup.env** + backup_data:/var/backups + saját `backup.sh`/`cleanup.sh`/`crontab.txt` Alpine Dockerfile-ban, **explicit 'app-network' bridge network**, restart policy `unless-stopped`). **Depends on:** 1.8
- [x] **Task 1.2:** Spring Boot 3.3+ projekt inicializálása Java 21 LTS verzióval (**Maven** build tool, **Spring Boot 3.3.5**, groupId `hu.tanszek.device`, artifactId `device-storage-backend`, version `0.1.0`, multi-stage Dockerfile [maven:3.9-eclipse-temurin-21 → eclipse-temurin:21-jre-alpine, non-root user, healthcheck]). **pom.xml** 24 dependency-vel: core Spring Boot starters (Web, Data JPA, Security, Validation, Actuator), PostgreSQL, Flyway (core + postgresql), Springdoc OpenAPI 2.6.0, Bucket4j 8.10.1, spring-retry, Spring Mail, **spring-boot-starter-actuator** (health/info/refresh), OpenTelemetry Spring Boot starter + Micrometer Tracing bridge, Spring Cloud Context (RefreshScope), Lombok, Apache POI 5.3.0, logstash-logback-encoder 7.4, Spring Boot Test, spring-security-test, Testcontainers (postgresql, junit-jupiter), H2 (test scope). **Spotless + Checkstyle + Jacoco (70% coverage gate)** konfigurálva. **Application.yml + application-dev.yml + application-prod.yml** + logback-spring.xml (structured JSON prod-ban) + messages_hu.properties + messages_en.properties placeholder-ek. **Depends on:** —
- [x] **Task 1.3:** Flyway `V1__init_schema.sql` megírása (11 tábla + 3 join table: configs, permissions, roles, locations [version optimistic lock + parent_id self-reference + type ENUM check constraint], app_users [must_change_password, password_changed_at, failed_login_count, locked_until + email_hash unique index], softwares [license_key_encrypted], devices [status ENUM check + inventory_number unique], device_softwares [join table], device_attachments [mime_type max 100, size_bytes, storage_path, cascade delete on device], device_assignments [egyetlen tábla + active flag + status ENUM + 8 FK-k + partial indexes on active=true], audit_logs [entity_type, entity_id, http_status, changes_json TEXT + descending timestamp index], refresh_tokens [token_hash unique + replaced_by_id ON DELETE SET NULL + partial index on revoked=true + 14 explicit indexes total]). **Classpath:** `backend/src/main/resources/db/migration/`. **`flyway.baseline-on-migrate: false`** + **`spring.flyway.repair-on-migrate: false`** + **Nincs Flyway undo**. **14 CREATE TABLE, 27 CREATE INDEX, 22 FK constraint, 304 sor.** **Depends on:** 1.2
- [x] **Task 1.4:** Flyway `V2__seed.sql` (**14 permission**, **3 role** + role_permissions mapping [ADMIN: minden, TEACHER: 6 permission, STUDENT: 3 permission], **hierarchikus location seed** [1 root OFFICE "Tanszéki Iroda" + 3 child: CLASSROOM "Tanterem 101", STORAGE "Eszköz Raktár", GROUP "Hallgatói Csoport" — DO $$ blokk + RETURNING id], **demo admin user** `admin@tanszek.local` / `ChangeMe123!` Argon2id placeholder hash-sel [implementációkor Argon2PasswordEncoder.encode() hívással generálandó] + `must_change_password=true`, **12 configs** [AUTH_PROVIDER, BACKUP_RETENTION_DAYS=30, AUDIT_RETENTION_YEARS=5, AUDIT_ARCHIVE_PATH, MAX_LOGIN_ATTEMPTS=5, LOCKOUT_DURATION_MIN=15, JWT_ACCESS_TTL_MIN=15, JWT_REFRESH_TTL_DAYS=30, JWT_KID_GRACE_PERIOD_SEC=3600, PAGINATION_DEFAULT_SIZE=20, PAGINATION_MAX_SIZE=50, ALERT_EMAIL_RECIPIENT]). **Depends on:** 1.3
- [x] **Task 1.5:** JPA Entitások + `BaseEntity` abstract class (`@MappedSuperclass`, `id`, `created_at`, `updated_at` JPA Auditing által töltve, `@EnableJpaAuditing` a backend-en, lombok @SuperBuilder) + 14 entitás (Config, Permission, Role, Location [@Version optimistic lock + parent self-reference + LocationType enum], AppUser [must_change_password + email_encrypted + email_hash + @ManyToMany permissions + @ManyToOne role + @ManyToOne officeLocation], Software [license_key_encrypted], Device [type String + status DeviceStatus enum + @ManyToMany softwares], DeviceAttachment [file_name + mime_type + size_bytes + storage_path + cascade delete device-re], DeviceAssignment [active flag + 8 FK + AssignmentStatus enum + state machine mezők], AuditLog [timestamp + user_email + endpoint + method + request_payload + changes_json + http_status + entity_type + entity_id], RefreshToken [token_hash SHA-256 + expires_at + revoked + replaced_by rotation chain + isExpired()/isActive() helper]) + 14 JpaRepository interface (Config/Permission/Role/Location [JpaSpecificationExecutor] /AppUser [JpaSpecificationExecutor + revokeAllRefreshTokensByUserId + findExpiredLocks] /Software/Device [JpaSpecificationExecutor] /DeviceAttachment/DeviceAssignment [JpaSpecificationExecutor] /AuditLog [deleteByTimestampBefore + findByTimestampBefore] /RefreshToken [findByTokenHash + findChainRoot + deleteOldTokens]). **3 enum osztály**: LocationType, DeviceStatus, AssignmentStatus. **Depends on:** 1.3
- [ ] **Task 1.6:** `backup` konténer implementálása (Alpine + bash + postgresql-client + dcron, non-root user, napi 02:00 pg_dump, **30 nap retention a `BACKUP_RETENTION_DAYS` env var-ból** [a backup konténer **saját `env_file: ./backup.env`** direktívával tölti be a `backup.env.example`-ből, default 30 nap — NEM a backend .env-jéből, hogy ne lássa a backend titkait], healthcheck, depends_on postgres-re). **Depends on:** 1.1
- [x] **Task 1.7:** Vite + React + TypeScript + shadcn/ui + Tailwind + next-themes projekt inicializálás (**Vite 6.x, React 18.3.1, TypeScript 5.6, TanStack Query v5, shadcn/ui CLI ready**). **package.json** 30+ dependency-vel: Radix UI primitívek (avatar, dialog, dropdown-menu, label, popover, select, separator, slot, toast, tooltip), axios, i18next + react-i18next, lucide-react, next-themes, react-error-boundary, react-router-dom 6, sonner, tailwind-merge, tailwindcss-animate, zod, zustand. **DevDeps:** TypeScript, ESLint, Prettier, Tailwind, PostCSS, Autoprefixer, vite plugin react. **vite.config.ts** Vite proxy `/api → localhost:8080`, path alias `@/`, manual chunks. **tailwind.config.js** shadcn/ui theme rendszer, CSS variables (light + dark mode). **postcss.config.js** Tailwind + autoprefixer. **index.html** + **.env.example** `VITE_*` prefix-szel. **Multi-stage Dockerfile** (node:20-alpine → nginx:1.27-alpine, healthcheck). **Részletes nginx.conf** (security headers [X-Frame-Options, CSP, X-Content-Type-Options, Referrer-Policy], gzip, /api/ reverse proxy, static asset caching 1y, SPA fallback `try_files`). **src/ struktúra:** App.tsx (provider-ek: ErrorBoundary, I18nextProvider, ThemeProvider, QueryClientProvider, BrowserRouter), main.tsx, index.css (Tailwind + CSS vars), **lib/api/axios.ts** (Bearer + CSRF interceptor, refresh-in-progress lock + queue), **lib/i18n/i18n.ts** (hu + en, navigator locale detection), **lib/theme/theme-provider.tsx** (next-themes wrapper), **components/ui/button.tsx** (shadcn button cva variants), **components/SonnerWrapper/Toaster.tsx**, **components/ErrorBoundary.tsx**, **routes/index.tsx** (központi routes placeholder), **features/auth/pages/LoginPage.tsx** (placeholder i18n használattal), **lib/i18n/{hu,en}.json** (teljes i18n resource fájlok), **lib/i18n/i18n-keys.ts** (MessageKey union type placeholder, Task 2.8-ban code generator). **.eslintrc.json + .prettierrc**. **Depends on:** —
- [x] **Task 1.8:** Repo skeleton: `backend/`, `frontend/`, `docs/`, `scripts/` mappák, `.env.example`, `backup.env.example`, `.gitignore` (multi-stack Java/Node/IDE/Docker), `.editorconfig`, `LICENSE` (MIT), `CONTRIBUTING.md`, monorepo `README.md`, `bootstrap.sh` script (env másolás + JWT/Crypto secret generálás + Docker check, idempotens, tesztelve). **Depends on:** —
- [x] **Task 1.9:** AuthProvider interface + LocalAuthProvider (Argon2 + DB, SHA-256 email hash lookup, active/lockedUntil/password verify, failedLoginCount + 15min lockout, transparent rehash, GrantedAuthority-k role + user permissions uniójából) + StubAdAuthProvider placeholder (AuthenticationServiceException-t dob, AD integráció Future Work), AuthProviderFactory (`@RefreshScope`, `configs.AUTH_PROVIDER` olvasás, fallback LOCAL, `Map<String, AuthProvider>` lookup), SecurityConfig placeholder (Argon2PasswordEncoder bean + AuthenticationManager bean, TODO Task 2.3 a teljes Security Filter Chain-hez). **Depends on:** 1.5

### Fázis 2: Biztonság, Titkosítás, Auth & Audit Alapok

- [x] **Task 2.1:** `CryptoService` interface + `DefaultCryptoService` implementáció (AES-GCM 256 + SHA-256, base64 kódolt 32-byte kulcs a `CRYPTO_AES_KEY` env var-ból, fail-fast ha a kulcs hiányzik vagy nem 32 byte, random 12-byte IV minden encrypt hívásban az IND-CPA biztonságért, 128-bit GCM auth tag integritás-védelem, base64 kódolt `iv:ciphertext` formátum). `CryptoException` runtime exception. **Unit tesztek (10 db):** encrypt/decrypt round-trip, IND-CPA (10 encrypt → 10 különböző ciphertext), tampering detection, üres/null input, invalid key length. **Depends on:** 1.5
- [x] **Task 2.2:** Spring Security 6 + Argon2id + rehash logika (`LoginService.authenticate()` delegál az `AuthProviderFactory.getActiveProvider().authenticate()`-hez, majd sikeres login esetén `passwordEncoder.upgradeEncoding()` check + `encode(rawPassword)` rehash + DB update egy tranzakcióban). **`UserService.changePassword(userId, currentPassword, newPassword)`** (validáció: Argon2 current match, min 12 char, current != new, **bulk revoke refresh token-ek** security okokból). **`UserService.deactivate/reactivate`** (refresh token bulk revoke deactivation-kor). **`AuthController` `/api/auth/password-change` endpoint** (Validation, Authentication context, SecurityContext-ből emailHash → userId lookup). **2 új exception**: `BusinessValidationException` (messageKey + message), `ResourceNotFoundException`, `UnauthorizedActionException`. **1 DTO**: `PasswordChangeRequest` (record, @NotBlank + @Size validáció). **9 unit teszt** UserService-re: changePassword success + hibás current + túl rövid + same as current + not found, deactivate success + already deactivated, reactivate success + already active. **Depends on:** 1.5
- [x] **Task 2.3:** JWT auth rendszer (access 15min + refresh 30day HttpOnly Secure SameSite=Strict cookie-ban), kid alapú rotáció 1 óra grace period-dal, refresh token rotation reuse detection-nel (chain revoke), UserService.deactivate() bulk refresh token revoke. **JwtProperties** (@ConfigurationProperties jwt.* prefix, fail-fast kulcs hossz validáció ≥32 byte HS256), **JwtTokenProvider** (HS256 sign/verify, kid header, generateAccessToken(emailHash, role, permissions), validateTokenWithGracePeriod — active + previous key fallback), **RefreshTokenService** (issue/rotate/revoke/revokeEntireChain, random 32-byte Base64 URL-safe token, SHA-256 hash DB-ben), **JwtAuthenticationFilter extends OncePerRequestFilter** (Bearer header parse → validateTokenWithGracePeriod → CustomUserDetailsService.loadUserByUsername → UsernamePasswordAuthenticationToken + extractAuthorities(role+permissions)), **CustomUserDetailsService** (findByEmailHash + ROLE_ prefix + role.permissions + user.permissions unió). **AuthController bővítve**: POST /api/auth/login (LoginRequest → LoginResponse{accessToken, expiresIn} + refresh cookie Set-Cookie), POST /api/auth/refresh (cookie-ból → rotation → új access + új cookie), POST /api/auth/logout (revoke + cookie törlés), POST /api/auth/password-change (Task 2.2). **2 DTO**: LoginRequest (@Email + @NotBlank), LoginResponse (record). **SecurityConfig bővítve**: teljes SecurityFilterChain (CSRF/CORS kikapcsolva most — TODO, STATELESS session, /api/auth/* public, JwtAuthenticationFilter a BasicAuthenticationFilter előtt). **Depends on:** 1.5
- [x] **Task 2.4:** Refresh token cleanup `@Scheduled` (cron: `0 0 4 * * *` = napi 04:00 UTC). **7+ napos grace**: törli a lejárt tokeneket (expires_at < now) ÉS a revoked tokeneket grace után (revoked = true AND created_at < now - 7 nap). **Email alert on failure**: `ScheduledJobMonitoring` wrapper + `MailService` (@Async) — hiba esetén emailt küld az `ALERT_EMAIL_RECIPIENT`-nek (`[ALERT] Scheduled job failed: refresh-token-cleanup` subject + stacktrace body). **3 új osztály**: `MailService` (`JavaMailSender` + @Async + SimpleMailMessage), `ScheduledJobMonitoring` (run(jobName, runnable) wrapper), `RefreshTokenCleanupJob` (@Scheduled + doCleanup metódus). **Native SQL query** az `RefreshTokenRepository.deleteOldTokens()`-ban (JPQL OR korlátok miatt). **`@EnableAsync`** a DeviceStorageApplication osztályban. **3 unit teszt**: cleanupCallsRepositoryWith7DayGraceCutoff, cleanupWithNoTokensDeletesNothing, cleanupOnExceptionSendsAlertEmail. **Depends on:** 2.3
- [x] **Task 2.5:** Bucket4j rate limiting (login per-IP **5/perc**, per-email **10/óra**) + account lockout (5 próba → 15min Task 2.2 LocalAuthProvider). **RateLimitFilter extends OncePerRequestFilter** (`@Order(1)`, Security Filter Chain 2. lépése CsrfFilter ELŐTT, in-memory `ConcurrentHashMap<String, Bucket>` per-IP és per-email bucket-ek, X-Forwarded-For header támogatás, 429-es JSON válasz `Retry-After: 60` header-rel). **RateLimiterService**: per-IP `Bandwidth.classic(5, Refill.intervally(5, 1min))`, per-email `Bandwidth.classic(10, Refill.intervally(10, 1h))`, SHA-256 hash az email kulcsnak. **GlobalExceptionHandler** (`@RestControllerAdvice`): 6 exception handler (BusinessValidationException → 400/429, ResourceNotFoundException → 404, UnauthorizedActionException → 401/403, AuthenticationException → 401, MethodArgumentNotValidException → 400 + field error lista, Exception fallback → 500). Egységes JSON body: `{timestamp, status, error, messageKey, message, path, details}`. **6 unit teszt** RateLimiterService-hez: per-IP limit, per-email limit, független bucket-ek, null/blank input. **Depends on:** 2.2 (SecurityConfig-ba kell regisztrálni a Filter láncban).
- [x] **Task 2.6:** `@RequirePermission` custom annotáció (`@Target(METHOD)`, `@Retention(RUNTIME)`, `String value()`) + `RequirePermissionAspect` (`@Aspect @Around`, SecurityContext-ből olvassa a GrantedAuthority-ket, `UnauthorizedActionException("permissionDenied")` ha nincs meg a permission, `UnauthorizedActionException("authRequired")` ha nincs bejelentkezve). **SecurityConfig bővítve**: `RoleHierarchy` bean (`ROLE_ADMIN > ROLE_TEACHER > ROLE_STUDENT` impl). **3 unit teszt** az aspektushoz: allowsMethodWhenUserHasPermission, throwsExceptionWhenUserMissingPermission, throwsExceptionWhenUnauthenticated (Mockito mock a ProceedingJoinPoint-ra). **Depends on:** 1.2
- [x] **Task 2.7:** AOP Audit Interceptor (entity_type + entity_id capture, maszkolás case-insensitive, **changes_json formátum: `{"before": {...}, "after": {...}}`**). **`@AuditTarget` annotáció** (`@Target(METHOD)`, `entityType + action` paraméterek). **`AuditAspect`** `@Around` advice service-szinten (`within(hu.tanszek.device..service..*)` pointcut): BEFORE state DB query, service metódus futtatás, AFTER state serialize, audit log mentés @Async + ScheduledJobMonitoring wrapper alert-tel. **Érzékeny mező maszkolás**: case-insensitive substring match (password, secret, token, licensekey) → ***. **5 unit teszt** az AuditAspect-re: maskSensitiveFieldsReplacesPassword, maskSensitiveFieldsIsCaseInsensitive, maskSensitiveFieldsKeepsNonSensitiveFields, buildChangesJsonProducesBeforeAfterStructure, buildChangesJsonReturnsNullWhenBeforeAndAfterIdentical (skip audit ha nincs változás). **Depends on:** 1.2
- [x] **Task 2.8:** Backend i18n MessageSource ResourceBundle hu+en, camelCase kulcsok (33 kulcs: validation.*, auth.*, crud, password.*, refresh.*, rateLimitExceeded, permissionDenied, deviceNotFound, stb.). **`GenerateI18nKeys` Maven exec plugin** (`org.codehaus.mojo:exec-maven-plugin:3.3.0`, `process-resources` phase, Java main class: `hu.tanszek.device.buildtools.GenerateI18nKeys`, 3 argumentum: hu.properties + en.properties + kimeneti TypeScript fájl). A script beolvassa mindkét properties fájlt **UTF-8 kódolással** (`InputStreamReader` + `StandardCharsets.UTF_8`), rendezi a kulcsokat, és generálja a TypeScript union type-ot + `defaultMessages` dict-et. **Kimenet:** `frontend/src/lib/i18n/i18n-keys.ts` (80 sor, 32 kulcs, escape-elt string literálok). **Tesztelve:** lokálisan futtatva a Java compile + script, 32 kulcs helyesen generálva, magyar karakterek UTF-8-ban helyesen. **Depends on:** 1.2
- [x] **Task 2.9:** Structured JSON logging (Logback + logstash-logback-encoder) MDC-vel: **request_id** (RequestIdFilter), **user_id, user_email, endpoint, method, timestamp, trace_id, span_id** (OpenTelemetry + Micrometer Tracing), OTLP exporter dev → console, prod → env. **RequestIdFilter extends OncePerRequestFilter** (`@Order(0)` Security Filter Chain legeleje): UUID `request_id` generálás VAGY kliens `X-Request-ID` header átvétel (validáció: max 64 karakter + regex `[a-zA-Z0-9-]+` log injection védelem), MDC.put/remove, response `X-Request-ID` header. **logback-spring.xml** bővítve: prod profile JSON (`LoggingEventCompositeJsonEncoder` + `<mdc>` provider + service='device-storage-backend' pattern), dev profile human-readable (`%X{request_id:-N/A}` MDC lookup). **AsyncConfig** (`AsyncConfigurer`): ThreadPoolTaskExecutor + TaskDecorator ami átmásolja az MDC-t a háttér-thread-re (@Async metódusok log-jai is tartalmazzák a request_id-t). **6 unit teszt** RequestIdFilter-re: generatesUuidWhenClientHeaderMissing, acceptsValidClientRequestId, rejectsInvalidClientRequestId (XSS prevention), rejectsRequestIdLongerThan64Chars, mdcClearedAfterFilter, emptyHeaderGeneratesNewUuid. **Depends on:** 1.2
- [x] **Task 2.10:** Mail konfig (Spring Mail + **Spring Retry @Retryable** + **@EnableRetry**, **@Async** MailService.sendSimpleEmail, dev → MailHog :1025, prod → env SMTP, **3 attempt exponential backoff: 1s → 2s → 4s**, configurable: `mail.retry.{max-attempts, initial-delay, multiplier, max-delay}` properties). AtomicInteger counters (totalAttempts, totalFailures) monitoring célokra. 4 unit teszt MailService-re. **`@EnableRetry`** a DeviceStorageApplication osztályban. **Depends on:** 1.1

### Fázis 3: Üzleti Logika, CRUD, Excel Import & Hibakezelés

- [ ] **Task 3.1:** GlobalExceptionHandler (`@RestControllerAdvice` + `@ExceptionHandler` minden exception típushoz: BusinessValidationException, ResourceNotFoundException, UnauthorizedActionException, OptimisticLockException, MethodArgumentNotValidException, Exception fallback) + egységes JSON body (timestamp/status/error/message/messageKey/path/details) + **FieldError lista a details-ben** validation hibáknál (`{field, message, rejectedValue}`) + **Admin unlock endpoint `POST /api/admin/users/{id}/unlock`** (USER_MANAGE permission, `failed_login_count = 0` + `locked_until = NULL`, audit log). **Depends on:** 1.5
- [x] **Task 3.2:** Repositories JpaSpecificationExecutor-ral + Pageable, **row-level szűrés MINDEN műveletre** (DEVICE_READ/UPDATE/DELETE/ASSIGN/UNASSIGN). **`DeviceSpecifications.hasAccess(currentUserId)`** Specification builder: ADMIN (currentUserId=null) → minden device (cb.conjunction), STUDENT/TEACHER → EXISTS subquery a `device_assignments.to_user_id = currentUser AND active = true` feltétellel. **`DeviceQueryService.findAllForCurrentUser(currentUserId)`** a Specification Executor-t használja. **TEACHER-specifikus irodai szűrés TODO** (device.location_id = user.office_location_id) — Task 3.3-ban implementálva a DeviceService-be. A `@Specification` subquery megközelítéssel a Device-hoz JOIN nélkül tudunk szűrni a device_assignments táblára. **Depends on:** 2.6
- [x] **Task 3.3:** Assign/unassign service state machine-nel + kétlépcsős approval flow-val. **State machine:** IN_STORAGE → PENDING_ASSIGNMENT (assign kérés) → ASSIGNED (admin approve) → PENDING_UNASSIGNMENT (unassign kérés) → IN_STORAGE (admin approve). **`DeviceService.requestAssignment(deviceId, targetLocationId, targetUserId, byUserId)`** — strict validation (device.status NEM MAINTENANCE/DISPOSED, location.type NEM GROUP forrás ÉS cél, user valid), régi aktív assignment inaktiválása, új PENDING_ASSIGNMENT rekord (active=false). **`DeviceService.approveAssignment(assignmentId, approvedByUserId)`** — PENDING_ASSIGNMENT → ASSIGNED átmenet, device.status=ASSIGNED. **`DeviceService.requestUnassignment(assignmentId, byUserId)`** — PENDING_UNASSIGNMENT kérés. **`DeviceService.approveUnassignment(unassignmentId, approvedByUserId)`** — IN_STORAGE-ra váltás, device.status=IN_STORAGE. **@AuditTarget annotáció** minden metóduson (entityType='DeviceAssignment', action='assign'/'approve_assign'/'unassign'/'approve_unassign'). **11 unit teszt**: requestAssignment success + 4 failure case, approveAssignment success + 1 failure, requestUnassignment success + 1 failure, approveUnassignment success. **Depends on:** 2.6
- [x] **Task 3.4:** Excel import service (Apache POI, users + devices, idempotens **UPDATE-OR-SKIP** logika: email_hash / inventory_number alapján). **Külön `com.tanszek.device.import_` feature package** (ImportController + ImportService + 4 DTO + synchronized concurrency). **Upload → Preview → Confirm flow**: `POST /api/import/preview` (Excel feltöltés, száraz validáció) → `POST /api/import/execute` (preview response visszaküldése, tényleges import). **Apache POI WorkbookFactory.create(InputStream)** — Sheet 0: Users (email, firstName, lastName, role, active, officeLocationName), Sheet 1: Devices (inventoryNumber, type, status, locationName). **Bean Validation** minden DTO-ra (`@NotBlank`, `@Email`, `@Size`, `@Pattern`). **Idempotens logika**: user email_hash (SHA-256) → findByEmailHash → ha létezik UPDATE, ha nem INSERT; device inventory_number → findByInventoryNumber → UPDATE-or-INSERT. **Concurrency:** synchronized blokk (`importLock`) — egyszerre csak egy import, többire 409 Conflict. **Biztonság:** @RequirePermission('USER_MANAGE') mindkét endpointon + mime check (xlsx) + max 10MB (Task 3.8-ban implementálva). **@Transactional** az execute-n — rollback ha bármelyik sor fail. **2 DTO input:** ImportUserRow (record), ImportDeviceRow (record). **2 DTO output:** ImportPreviewResponse (validUsers + validDevices + invalidRows nested record), ImportResult (counts: inserted/updated/errors). **Depends on:** 1.5
- [x] **Task 3.5:** Device attachments service (upload + delete + deleteByDevice). **Fájl tárolás:** `./uploads/devices/{device_id}/{uuid}.{ext}` lokális volume mount-on (`uploads_data:/var/uploads`). **Limits:** max 5MB/fájl (`MAX_FILE_SIZE`), max 5/device (`MAX_FILES_PER_DEVICE`), **strict mime whitelist**: image/jpeg, image/png, image/gif, image/webp, application/pdf, application/msword, application/vnd.openxmlformats-officedocument.* (docx/xlsx), text/plain. **AttachmentService.upload(deviceId, file, uploadedByUserId)** — 6 validáció: device+user létezik, fájlméret <= 5MB, mime type whitelist, device-onkénti fájlszám < 5, fizikai tárolás UUID-vel, DB rekord. **AttachmentService.delete(attachmentId)** — DB + fizikai fájl törlése (ha a fájl törlése fail, a DB rekord akkor is törlődik). **AttachmentService.deleteByDevice(deviceId)** — cascade policy: device törlésekor az összes attachment rekord + fájl törlődik. **AttachmentService.findByDevice(deviceId)** — read-only listázás. **@AuditTarget** minden művelethez. **Depends on:** 1.5
- [x] **Task 3.6:** Audit retention `@Scheduled` vasárnap 03:00 (`cron = "0 0 3 ? * SUN"`). **Retention policy:** 1+ éves rekordok archiválása + 5+ éves rekordok törlése. **NDJSON.gz formátum** exportálás (GZIPOutputStream + newline-delimited JSON) a `/var/backups/archive/audit/YYYY/audit-YYYY-MM-DD.ndjson.gz` útvonalra (audit_archive_data volume mount). **Rekordok csoportosítása évente** (HashMap<Integer, List<AuditLog>>), minden évhez külön fájl. **Email alert on failure** a ScheduledJobMonitoring wrapper-en keresztül. **Config-ból olvassa:** `audit.archive-path` (default /var/backups/archive/audit) és `audit.retention-years` (default 5). **AuditRetentionJob**: archiveOldRecords() — findByTimestampBefore + recordsByYear map + writeArchiveFile() GZIP-pel; executeRetention() — archiveCutoff = now - 365 nap, deleteCutoff = now - retentionYears * 365 nap. **Depends on:** 1.6, 2.7
- [x] **Task 3.7:** Audit rollback endpoint `POST /api/audit/rollback/{id}` (AUDIT_ROLLBACK permission, supported entity types: **Device, User, Location, Assignment, Software, Attachment**, új audit log generálás, **@Transactional REQUIRED, all-or-nothing**). **EntityTypeRegistry** helper: entity_type string → repository.findById() map (Device/User/Location/Assignment/Software/Attachment), toJsonMap() az entitás → Map<String, Object> konverzió (mezőnevek + értékek), applyJsonMap() a Map → entitás in-place apply. **AuditRollbackService.rollback(auditLogId)**: changes_json parse (ObjectMapper.readValue), rollback típus meghatározása (UPDATE: after→before, CREATE: delete, DELETE: recreate), entity betöltés (EntityTypeRegistry), applyJsonMap(beforeState), új AuditLog rekord (rollbackOf=auditLogId). **AuditController** @RequirePermission('AUDIT_ROLLBACK') + POST /api/audit/rollback/{id}. CREATE és DELETE rollback TODO Task 3.7+ (jelenlegi TODO kommentek a rollbackCreate és rollbackDelete metódusokban). **Depends on:** 2.7
- [x] **Task 3.8:** Request méretkorlát háromszintű — **Nginx** `client_max_body_size 10M` a /api/ location block-ban (Task 1.1-ben), **Spring multipart** `max-file-size=10MB` + `max-request-size=10MB` (Task 1.2 application.yml), **Bean Validation** `@Size` minden DTO String mezőn (mostantól). **`SizeLimits` konstans osztály** (common/) — egységes limitek: SHORT_TEXT_MAX=100, MEDIUM_TEXT_MAX=255, LONG_TEXT_MAX=500, VERY_LONG_TEXT_MAX=10000, INVENTORY_NUMBER_MAX=50, EMAIL_MAX=255, PASSWORD_HASH_MAX=255, URL_MAX=2048, ENDPOINT_MAX=500, MESSAGE_KEY_MAX=100, FILE_NAME_MAX=255, MIME_TYPE_MAX=100, STORAGE_PATH_MAX=500. **3 DTO refaktorálva** SizeLimits-ra: ImportUserRow, ImportDeviceRow, PasswordChangeRequest. **Depends on:** 1.1, 1.5
- [x] **Task 3.9:** Springdoc OpenAPI integráció (`springdoc-openapi-starter-webmvc-ui:2.6.0` Task 1.2-ből + **OpenApiConfig** bean). **Project info**: title='Device Storage API', version='0.1.0', description='JWT auth + row-level + audit + Excel', MIT license, contact=tanszek admin. **JWT bearer security scheme**: 'Authorization: Bearer {jwt}' a /v3/api-docs JSON + /swagger-ui.html UI-hoz. **Endpointok**: `/v3/api-docs` JSON, `/swagger-ui.html` Web UI (dev profile). Controller @Operation/@ApiResponse annotációk Task 4.x-ben (controller implementáláskor). **Depends on:** 2.3
- [ ] **Task 3.10:** **Application.yml konfiguráció (§8 a tervben):** `application.yml` (közös), `application-dev.yml` (mailhog, debug log), `application-prod.yml` (env SMTP, OTLP endpoint, info log), profile szétválasztás `SPRING_PROFILES_ACTIVE` env var-ból. **Depends on:** 2.10

### Fázis 4: Frontend Integráció & UX

- [x] **Task 4.1:** Axios HTTP kliens + Zustand store + moduláris API endpoint-ok. **`useAuthStore`** (Zustand create<AuthState>): accessToken, userEmail, role, permissions[], mustChangePassword state + setAuth/setAccessToken/clearAuth/setMustChangePassword actions + getAccessToken/setAccessToken helper-ek. **Axios kliens** (`apiClient`): withCredentials:true, request interceptor (Bearer token a Zustand store-ból, X-XSRF-TOKEN cookie-ból state-changing kéréseknél), response interceptor (401 silent refresh: refresh-in-progress lock + queue, POST /api/auth/refresh, retry az eredeti kéréssel, no warning modal, refresh failure → clearAuth + /login redirect, /api/auth/ endpointokon NE próbálkozzon). **`authApi`** (features/auth/api/): login(), refresh(), logout(), changePassword() + LoginRequest/LoginResponse/PasswordChangeRequest type-ok. **`deviceApi`** (features/device/api/): findAll/findById/create/update/delete + Device/PageRequest/PageResponse type-ok. **Depends on:** 2.3
- [x] **Task 4.2:** i18next bekötés + teljes UI i18n resource fájlok + LanguageSelector komponens. **`hu.json` + `en.json` kibővítve** (~150+ kulcs, 11 kategória: common, nav, login, passwordChange, dashboard, admin, devices, users, locations, assignments, audit, import, attachments, permissions, roles, messages). A backend 32 messageKey mapping-je a frontend `messages.*` keys alatt. **`LanguageSelector` komponens** (components/LanguageSelector.tsx): useTranslation hook + lucide-react `Languages` icon + 2 gomb (HU 🇭🇺 / EN 🇬🇧), i18n.changeLanguage + localStorage.setItem('device-storage-locale', code). A Language prioritás a `lib/i18n/i18n.ts`-ben maradt (user choice > localStorage > navigator.language > 'hu' fallback). **Depends on:** 2.8
- [x] **Task 4.3:** Login oldal + first-login jelszócsere (**React Hook Form + Zod schema validáció**, **inline Dialog modal** a Dashboard-en, ha `mustChangePassword=true`). **`LoginForm`** (features/auth/components/): useForm + zodResolver(loginSchema), email + password, Sonner toast hiba/siker, useAuthStore.setAuth() + navigate('/my-dashboard') siker esetén. **`PasswordChangeForm`** (features/auth/components/): Dialog (radix-ui), useForm + zodResolver(passwordChangeSchema) — currentPassword + newPassword (min 12 + regex: kis/nagy/szám/speciális) + confirmNewPassword (refine: equality check), useAuthStore.setMustChangePassword(false) siker esetén. **Zod schemák**: loginSchema (email validáció), passwordChangeSchema (5 feltétel: min 12, kis, nagy, szám, speciális, + új ≠ régi, confirm match). **`LoginPage`** bővítve: appName + subtitle + LoginForm + LanguageSelector + themeToggle (lucide-react Sun/Moon). **`Dialog`** shadcn UI komponens (radix-ui dialog wrapper). **`Label`** + **`Input`** shadcn UI komponensek. **`DashboardPage`** placeholder (Task 4.4-ben layout impl). **Frontend `sizeLimits.ts`** a backend SizeLimits tükörképe. **`@hookform/resolvers` + `react-hook-form`** dependency-k a package.json-ban. **routes/index.tsx**: ha nincs token → redirect /login, ha mustChangePassword → modal, ha be van jelentkezve → Dashboard. **Depends on:** 4.1, 4.2
- [x] **Task 4.4:** Szerepkörfüggő dashboardok + route védelem. **Wrapper komponensek** (`components/auth/`): RequireAuth (redirect /login ha nincs token), RequireRole (redirect /403 ha nincs role), RequirePermission (redirect /403 ha nincs permission). **MyDashboardPage** (`features/auth/pages/`): 3 Card (myDevices, myLocations, recentAssignments) DashboardPage wrapper-ben. **AdminLayout** (`features/admin/layouts/`): Sidebar (lucide-react ikonok: LayoutDashboard, Users, Laptop, MapPin, Shield, FileText, Upload, **224px széles**) + Header + Main content (children/Outlet). NavLink aktív státusszal. **ForbiddenPage** (403): permissionDenied üzenet, dashboard link. **routes/index.tsx** 4 state: nincs token (login redirect), mustChangePassword (modal), normál authenticated (RequireAuth + RequireRole). **Card shadcn UI** komponens (Card, CardHeader, CardTitle, CardDescription, CardContent). **Depends on:** 4.3
- [x] **Task 4.5:** Lapozható, szűrhető **TanStack Table v8** + shadcn table komponens + Devices tábla. **`DataTable<TData>`** generic komponens (components/DataTable/): TanStack v8 useReactTable, manualPagination + manualFiltering, getCoreRowModel/getSortedRowModel/getFilteredRowModel/getPaginationRowModel, SortingState + ColumnFiltersState, Search bar (Search ikon + Input), Pagination controls (ChevronsLeft/Left/Right/Right ikonok + Button), i18n (page, of, itemsPerPage, loading, noData, search, previous, next). **`Table` shadcn UI** (Table, TableHeader, TableBody, TableRow, TableHead, TableCell, TableCaption) Tailwind utility-kkel. **`DevicesPage`** (features/device/pages/): useQuery + useState page/search, deviceApi.findAllDevices({page, size, filter}), 5 oszlop (id, inventoryNumber, type, status badge, actions: FileText/Pencil/Trash2 ikonok + Link), Pageable totalElements + totalPages a szerver-oldali lapozáshoz, Search inventoryNumber-re, "New Device" gomb Plus ikonnal. **`@tanstack/react-table@8.20.5`** dependency. **Depends on:** 3.2, 4.1
- [x] **Task 4.6:** Excel import UI (Upload → Preview → Confirm flow + **drag-drop + CSV export**). **`react-dropzone@14.2.10`** dependency. **`importApi`** (features/import/api/): previewImport(file, FormData) → ImportPreviewResponse, executeImport(preview) → ImportResult. **`ImportPage`** (features/import/pages/): 3 state machine (EMPTY/PREVIEW/RESULT), useDropzone (accept: xlsx, maxSize: 10MB, maxFiles: 1), useMutation preview + useMutation execute, Sonner toastok, Badge-ek (green: valid, destructive: invalid), InvalidRowItem komponens (row number, raw data, errors lista), ResultMetric komponens (5 metrika: usersInserted/Updated, devicesInserted/Updated, errors), **CSV export** invalid rows-ból (Blob + URL.createObjectURL + download), **inline hibák** (piros border, errors listája), **Cancel + Reset** gombok, **/admin/import** route. **`@tanstack/react-query`** useMutation hook-ok. **`Badge`** + **`Separator`** shadcn UI komponensek. i18n kulcsok: totalRows, rows. **Depends on:** 3.4, 4.1
- [ ] **Task 4.7:** Device attachments UI (drag-drop upload, preview grid, delete, kliens-oldali mime/size validation [5MB, elfogadott mime típusok]). **Depends on:** 3.5, 4.5
- [ ] **Task 4.8:** Audit log viewer (szűrés, lapozás, diff side panel pretty-print, rollback gomb jogosult entitásoknál). **Depends on:** 3.7, 4.5
- [x] **Task 4.10:** Backend REST controller-ek a frontend hívásaihoz (DeviceController, LocationController, UserController, AuditController) — lapozás + szűrés + CRUD + DTO-k + @RequirePermission. **In progress: 4/4 controller kész** (Device, Location, User, Audit). Depends on: 1.5.
- [x] **Task 4.7:** Device attachments UI (drag-drop upload + preview grid + delete). **`attachmentApi`**: findByDevice, upload (FormData, max 5MB), deleteAttachment, formatFileSize helper (B/KB/MB). **`DeviceDetailPage`**: useParams + useQuery device + attachments, react-dropzone (accept: image/*, application/pdf, msword, xlsx, text/plain, maxSize 5MB, maxFiles 1), useMutation upload + delete (invalidate cache), AttachmentItem komponens (FileText ikon, mime type Badge, file size, uploaded at, Trash2 delete gomb). **5/5 device limit megjelenítve** (X/5 counter). **Depends on:** 3.5, 4.1.

### Fázis 5: Tesztelés & QA

- [ ] **Task 5.1:** Unit tesztek (service réteg, min. 70% lefedettség Jacoco, row-level filter minden műveletre kitesztelve, license key maszkolás mindkét ág). **Depends on:** 1.5
- [ ] **Task 5.2:** Testcontainers setup (pom.xml dependency, PostgresContainer shared instance, base teszt osztály). **Depends on:** 1.2
- [ ] **Task 5.3:** Integration tesztek (@SpringBootTest + MockMvc: auth flow, CRUD, jogosultság, rate limit, rollback, refresh rotation reuse detection, row-level filter írásra is, attachment cascade delete, kid grace period). **Depends on:** 5.2
- [ ] **Task 5.4:** Standard runbook (`docs/runbook.md`, **7 szekció**): (1) Első telepítés (bootstrap.sh, demo admin, jelszócsere), (2) Napi üzemeltetés (backup ellenőrzés, scheduled job státusz, log-ok), (3) Gyakori hibák + megoldás (DB connection, JWT invalid, 429, upload túl nagy, rollback permission), (4) Rollback eljárás (POST /api/audit/rollback — **nincs Flyway undo**, rollback az audit log changes_json-ból), (5) Backup restore (scripts/backup-restore.sh), (6) Credential rotation (scripts/rotate-jwt-secret.sh), (7) Incident response (UserService.deactivate, refresh token bulk revoke, audit log export). **Depends on:** 4.9
- [ ] **Task 5.5:** GitHub Actions workflow (`.github/workflows/ci.yml`: Spotless + Checkstyle + OWASP párhuzamosan → Unit (Jacoco ≥70%) → Integration → Docker build → Smoke test, status check). **SOLID code review checklist a workflow-ban:** SRP (minden service egy felelősség, nincs "god service"), DIP (repository-k interface-en át injektálva, mock-olhatók), OCP (új feature-ök extension pointokon mennek, nem módosítják a meglévő kódot). **Depends on:** 5.3

---

## Future Considerations (Out of Scope, későbbre)

*   **Argon2id → modernebb paraméterek** (memory/iterations növelés) - rehash logika már támogatja
*   **Jaeger/Tempo OTLP endpoint** production-ben (most csak console, dev)
*   **OWASP Dependency-Check report publikálás** (GitHub Security tab)
*   **Mobile app** (React Native / Flutter) - eszköz QR-kód olvasással
*   **2FA / MFA** (TOTP, SMS) - második faktor a login-hoz
*   **OIDC / SSO integráció** (Keycloak, university SAML)
*   **AD/LDAP integráció** (StubAdAuthProvider → tényleges LDAP connector)
*   **Email értesítések user-eknek** (assign/unassign confirmation, lockout notification, password expiry warning)
*   **Push notification** (böngésző push API)
*   **Dashboard analitika** (Chart.js / Recharts, riportok, statisztikák)
*   **Asset QR-kód generálás és nyomtatás** (eszközök fizikai címkézése)
*   **Bulk Excel export** (jelenlegi állapot exportálása, pl. éves leltár)
*   **Maintenance ticket rendszer** (MAINTENANCE státuszú eszközökhöz kapcsolódó javítási workflow)
*   **HA deployment** (Redis-backed Bucket4j, multi-instance backend, read replica DB)
*   **PWA / offline mód** (service worker, install-to-home-screen)
*   **Soft delete** entitásokon (most hard delete van)
*   **WebSocket real-time notifications** (assign/unassign azonnali push a dashboard-ra)
*   **Audit log viewer dashboard analitika** (trend riportok, gyanús activity detekció)
*   **Bulk operations** (tömeges device assign/unassign Excel-ből, nem csak import)

---

## Megjegyzések

*   A projekt jelenleg **tervezési fázisban** van, 0% haladás.
*   Single-developer scope (agent), reálisan 2-3 hét a teljes scope-hoz (nem "pár nap").
*   A taskok a `Depends on:` mezők alapján párhuzamosíthatók ahol a dependency megengedi.
*   **Fázis párhuzamossági mapping:**
    *   Fázis 1 → Fázis 2 (szekvenciális, Fázis 2 függ Fázis 1-től)
    *   Fázis 2 → Fázis 3 (szekvenciális)
    *   Fázis 3 ↔ Fázis 4 (párhuzamosan indítható, ha a task dependency megengedi: pl. Task 4.5 [DataTable] futhat a 3.4 [Excel import service] mellett, mert 3.2 már kész)
    *   Fázis 4 → Fázis 5 (szekvenciális, Fázis 5 a teljes scope-ra épít)
*   **Indítás:** Task 1.8 (repo skeleton, .env.example) → 1.1 (docker-compose) → 1.2/1.7 (backend + frontend init párhuzamosan) → 1.3 (Flyway V1) → 1.4 (Flyway V2 seed) → 1.5 (JPA entitások) → ...
*   **Kritikus útvonal:** 1.1 → 1.2 → 1.3 → 1.5 → 2.6 → 3.2 → 4.5 → 5.1 → 5.3 → 5.5 (a deployment és CI-hez elengedhetetlen lánc).
